<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Stable stays offline-pure: strip the opt-in Gemini INTERNET permission
         so the buildStable CI gate passes. Gemini engine falls back to Whisper here. -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />

    <application>
        <service
            android:name="org.futo.inputmethod.updates.UpdateCheckingService"
            android:label="@string/autoupdater_service_title"
            android:permission="android.permission.BIND_JOB_SERVICE" >
        </service>

        <receiver android:name="org.futo.inputmethod.updates.InstallReceiver" />

    </application>
</manifest>