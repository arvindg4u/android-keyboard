<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Unstable keeps the UpdateCheckingService/InstallReceiver from the
         stable overlay but WITHOUT the INTERNET strip, so the opt-in Gemini
         engine works in nightly/unstable builds. -->
    <application>
        <service
            android:name="org.futo.inputmethod.updates.UpdateCheckingService"
            android:label="@string/autoupdater_service_title"
            android:permission="android.permission.BIND_JOB_SERVICE" >
        </service>

        <receiver android:name="org.futo.inputmethod.updates.InstallReceiver" />

    </application>
</manifest>
